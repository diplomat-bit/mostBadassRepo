// REPOSITORY SOURCE: diplomat-bit/aibanking.dev-jocall3-new | PATH: diplomat-bit-aibanking.dev-jocall3-new-84d7a30/components/DownloadLink.tsx
================================================================================


import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      Download {filename}
    </a>
  );
};


================================================================================
// APPENDED FROM REPO: diplomat-bit/almost | ORIGINAL PATH: diplomat-bit-almost-93a5466/components/DownloadLink (1).tsx
================================================================================

import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      {filename}
    </a>
  );
};

================================================================================
// APPENDED FROM REPO: diplomat-bit/almost | ORIGINAL PATH: diplomat-bit-almost-93a5466/components/DownloadLink.tsx
================================================================================


import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      Download {filename}
    </a>
  );
};


================================================================================
// APPENDED FROM REPO: diplomat-bit/almost | ORIGINAL PATH: diplomat-bit-almost-93a5466/components/DownloadLink (2).tsx
================================================================================

import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      Download {filename}
    </a>
  );
};

================================================================================
// APPENDED FROM REPO: diplomat-bit/Fuckyou | ORIGINAL PATH: diplomat-bit-Fuckyou-70f83c5/components/DownloadLink_1.tsx
================================================================================


import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      Download {filename}
    </a>
  );
};


================================================================================
// APPENDED FROM REPO: diplomat-bit/Fuckyou | ORIGINAL PATH: diplomat-bit-Fuckyou-70f83c5/components/DownloadLink (1).tsx
================================================================================

import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      {filename}
    </a>
  );
};

================================================================================
// APPENDED FROM REPO: diplomat-bit/Fuckyou | ORIGINAL PATH: diplomat-bit-Fuckyou-70f83c5/components/DownloadLink.tsx
================================================================================


import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      Download {filename}
    </a>
  );
};


================================================================================
// APPENDED FROM REPO: diplomat-bit/Fuckyou | ORIGINAL PATH: diplomat-bit-Fuckyou-70f83c5/components/DownloadLink (2).tsx
================================================================================

import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      Download {filename}
    </a>
  );
};

================================================================================
// APPENDED FROM REPO: diplomat-bit/jamesburvelocallaghaniiiand | ORIGINAL PATH: diplomat-bit-jamesburvelocallaghaniiiand-9414e97/components/DownloadLink.tsx
================================================================================

import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      Download {filename}
    </a>
  );
};

================================================================================
// APPENDED FROM REPO: diplomat-bit/magic | ORIGINAL PATH: diplomat-bit-magic-a3f5ff1/components/DownloadLink (1).tsx
================================================================================

import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      {filename}
    </a>
  );
};

================================================================================
// APPENDED FROM REPO: diplomat-bit/magic | ORIGINAL PATH: diplomat-bit-magic-a3f5ff1/components/DownloadLink.tsx
================================================================================


import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      Download {filename}
    </a>
  );
};


================================================================================
// APPENDED FROM REPO: diplomat-bit/magic | ORIGINAL PATH: diplomat-bit-magic-a3f5ff1/components/DownloadLink (2).tsx
================================================================================

import React from 'react';

interface DownloadLinkProps {
  url: string;
  filename: string;
}

export const DownloadLink: React.FC<DownloadLinkProps> = ({ url, filename }) => {
  return (
    <a 
      href={url} 
      download={filename}
      className="text-blue-500 hover:text-blue-600 underline text-sm"
      target="_blank"
      rel="noopener noreferrer"
    >
      Download {filename}
    </a>
  );
};