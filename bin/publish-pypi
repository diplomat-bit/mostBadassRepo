// REPOSITORY SOURCE: diplomat-bit/aibank | PATH: diplomat-bit-aibank-3a68d63/bin/publish-pypi
================================================================================

#!/usr/bin/env bash

set -eux
rm -rf dist
mkdir -p dist
uv build
uv publish --token=$PYPI_TOKEN


================================================================================
// APPENDED FROM REPO: diplomat-bit/jocall3-python | ORIGINAL PATH: diplomat-bit-jocall3-python-03825e0/bin/publish-pypi
================================================================================

#!/usr/bin/env bash

set -eux
rm -rf dist
mkdir -p dist
uv build
uv publish --token=$PYPI_TOKEN
