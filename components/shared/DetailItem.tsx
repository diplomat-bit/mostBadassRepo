// REPOSITORY SOURCE: diplomat-bit/aibanking.dev-jocall3-new | PATH: diplomat-bit-aibanking.dev-jocall3-new-84d7a30/components/shared/DetailItem.tsx
================================================================================


import React from 'react';

export const DetailItem: React.FC<{ title: string; value: React.ReactNode; isMono?: boolean }> = ({ title, value, isMono }) => (
  <div className="flex justify-between py-1">
    <span className="text-gray-400 text-sm">{title}</span>
    <span className={`text-gray-200 text-sm ${isMono ? 'font-mono' : ''}`}>{value}</span>
  </div>
);


================================================================================
// APPENDED FROM REPO: diplomat-bit/almost | ORIGINAL PATH: diplomat-bit-almost-93a5466/components/shared/DetailItem.tsx
================================================================================


import React from 'react';

export const DetailItem: React.FC<{ title: string; value: React.ReactNode; isMono?: boolean }> = ({ title, value, isMono }) => (
  <div className="flex justify-between py-1">
    <span className="text-gray-400 text-sm">{title}</span>
    <span className={`text-gray-200 text-sm ${isMono ? 'font-mono' : ''}`}>{value}</span>
  </div>
);


================================================================================
// APPENDED FROM REPO: diplomat-bit/Fuckyou | ORIGINAL PATH: diplomat-bit-Fuckyou-70f83c5/components/shared/DetailItem.tsx
================================================================================


import React from 'react';

export const DetailItem: React.FC<{ title: string; value: React.ReactNode; isMono?: boolean }> = ({ title, value, isMono }) => (
  <div className="flex justify-between py-1">
    <span className="text-gray-400 text-sm">{title}</span>
    <span className={`text-gray-200 text-sm ${isMono ? 'font-mono' : ''}`}>{value}</span>
  </div>
);


================================================================================
// APPENDED FROM REPO: diplomat-bit/jamesburvelocallaghaniiiand | ORIGINAL PATH: diplomat-bit-jamesburvelocallaghaniiiand-9414e97/components/shared/DetailItem.tsx
================================================================================

import React from 'react';

export const DetailItem: React.FC<{ title: string; value: React.ReactNode; isMono?: boolean }> = ({ title, value, isMono }) => (
  <div className="flex justify-between py-1">
    <span className="text-gray-400 text-sm">{title}</span>
    <span className={`text-gray-200 text-sm ${isMono ? 'font-mono' : ''}`}>{value}</span>
  </div>
);

================================================================================
// APPENDED FROM REPO: diplomat-bit/magic | ORIGINAL PATH: diplomat-bit-magic-a3f5ff1/components/shared/DetailItem.tsx
================================================================================


import React from 'react';

export const DetailItem: React.FC<{ title: string; value: React.ReactNode; isMono?: boolean }> = ({ title, value, isMono }) => (
  <div className="flex justify-between py-1">
    <span className="text-gray-400 text-sm">{title}</span>
    <span className={`text-gray-200 text-sm ${isMono ? 'font-mono' : ''}`}>{value}</span>
  </div>
);
