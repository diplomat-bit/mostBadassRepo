// REPOSITORY SOURCE: diplomat-bit/almost | PATH: diplomat-bit-almost-93a5466/components/subscriptions/SubscriptionProductImage.tsx
================================================================================

import React from 'react';

interface SubscriptionProductImageProps {
  imageUrl: string;
  altText: string;
  className?: string;
}

const SubscriptionProductImage: React.FC<SubscriptionProductImageProps> = ({
  imageUrl,
  altText,
  className,
}) => {
  return (
    <div className={`relative ${className}`}>
      <img
        src={imageUrl}
        alt={altText}
        className="w-full h-auto object-cover rounded-lg shadow-md"
        loading="lazy"
      />
      {/* You can add overlays or badges here if needed for subscription status or promotions */}
    </div>
  );
};

export default SubscriptionProductImage;

================================================================================
// APPENDED FROM REPO: diplomat-bit/Fuckyou | ORIGINAL PATH: diplomat-bit-Fuckyou-70f83c5/components/subscriptions/SubscriptionProductImage.tsx
================================================================================

import React from 'react';

interface SubscriptionProductImageProps {
  imageUrl: string;
  altText: string;
  className?: string;
}

const SubscriptionProductImage: React.FC<SubscriptionProductImageProps> = ({
  imageUrl,
  altText,
  className,
}) => {
  return (
    <div className={`relative ${className}`}>
      <img
        src={imageUrl}
        alt={altText}
        className="w-full h-auto object-cover rounded-lg shadow-md"
        loading="lazy"
      />
      {/* You can add overlays or badges here if needed for subscription status or promotions */}
    </div>
  );
};

export default SubscriptionProductImage;

================================================================================
// APPENDED FROM REPO: diplomat-bit/jamesburvelocallaghaniiiand | ORIGINAL PATH: diplomat-bit-jamesburvelocallaghaniiiand-9414e97/components/subscriptions/SubscriptionProductImage.tsx
================================================================================

import React from 'react';

interface SubscriptionProductImageProps {
  imageUrl: string;
  altText: string;
  className?: string;
}

const SubscriptionProductImage: React.FC<SubscriptionProductImageProps> = ({
  imageUrl,
  altText,
  className,
}) => {
  return (
    <div className={`relative ${className}`}>
      <img
        src={imageUrl}
        alt={altText}
        className="w-full h-auto object-cover rounded-lg shadow-md"
        loading="lazy"
      />
      {/* You can add overlays or badges here if needed for subscription status or promotions */}
    </div>
  );
};

export default SubscriptionProductImage;

================================================================================
// APPENDED FROM REPO: diplomat-bit/magic | ORIGINAL PATH: diplomat-bit-magic-a3f5ff1/components/subscriptions/SubscriptionProductImage.tsx
================================================================================

import React from 'react';

interface SubscriptionProductImageProps {
  imageUrl: string;
  altText: string;
  className?: string;
}

const SubscriptionProductImage: React.FC<SubscriptionProductImageProps> = ({
  imageUrl,
  altText,
  className,
}) => {
  return (
    <div className={`relative ${className}`}>
      <img
        src={imageUrl}
        alt={altText}
        className="w-full h-auto object-cover rounded-lg shadow-md"
        loading="lazy"
      />
      {/* You can add overlays or badges here if needed for subscription status or promotions */}
    </div>
  );
};

export default SubscriptionProductImage;