// REPOSITORY SOURCE: diplomat-bit/almost | PATH: diplomat-bit-almost-93a5466/components/subscriptions/SubscriptionTaxInfo.tsx
================================================================================

import React from 'react';

interface SubscriptionTaxInfoProps {
  taxRate: number;
  taxAmount: number;
  currencySymbol: string;
}

const SubscriptionTaxInfo: React.FC<SubscriptionTaxInfoProps> = ({
  taxRate,
  taxAmount,
  currencySymbol,
}) => {
  return (
    <div className="subscription-tax-info">
      <p>
        Tax Rate: {taxRate.toFixed(2)}%
      </p>
      <p>
        Tax Amount: {currencySymbol}{taxAmount.toFixed(2)}
      </p>
    </div>
  );
};

export default SubscriptionTaxInfo;

================================================================================
// APPENDED FROM REPO: diplomat-bit/Fuckyou | ORIGINAL PATH: diplomat-bit-Fuckyou-70f83c5/components/subscriptions/SubscriptionTaxInfo.tsx
================================================================================

import React from 'react';

interface SubscriptionTaxInfoProps {
  taxRate: number;
  taxAmount: number;
  currencySymbol: string;
}

const SubscriptionTaxInfo: React.FC<SubscriptionTaxInfoProps> = ({
  taxRate,
  taxAmount,
  currencySymbol,
}) => {
  return (
    <div className="subscription-tax-info">
      <p>
        Tax Rate: {taxRate.toFixed(2)}%
      </p>
      <p>
        Tax Amount: {currencySymbol}{taxAmount.toFixed(2)}
      </p>
    </div>
  );
};

export default SubscriptionTaxInfo;

================================================================================
// APPENDED FROM REPO: diplomat-bit/jamesburvelocallaghaniiiand | ORIGINAL PATH: diplomat-bit-jamesburvelocallaghaniiiand-9414e97/components/subscriptions/SubscriptionTaxInfo.tsx
================================================================================

import React from 'react';

interface SubscriptionTaxInfoProps {
  taxRate: number;
  taxAmount: number;
  currencySymbol: string;
}

const SubscriptionTaxInfo: React.FC<SubscriptionTaxInfoProps> = ({
  taxRate,
  taxAmount,
  currencySymbol,
}) => {
  return (
    <div className="subscription-tax-info">
      <p>
        Tax Rate: {taxRate.toFixed(2)}%
      </p>
      <p>
        Tax Amount: {currencySymbol}{taxAmount.toFixed(2)}
      </p>
    </div>
  );
};

export default SubscriptionTaxInfo;

================================================================================
// APPENDED FROM REPO: diplomat-bit/magic | ORIGINAL PATH: diplomat-bit-magic-a3f5ff1/components/subscriptions/SubscriptionTaxInfo.tsx
================================================================================

import React from 'react';

interface SubscriptionTaxInfoProps {
  taxRate: number;
  taxAmount: number;
  currencySymbol: string;
}

const SubscriptionTaxInfo: React.FC<SubscriptionTaxInfoProps> = ({
  taxRate,
  taxAmount,
  currencySymbol,
}) => {
  return (
    <div className="subscription-tax-info">
      <p>
        Tax Rate: {taxRate.toFixed(2)}%
      </p>
      <p>
        Tax Amount: {currencySymbol}{taxAmount.toFixed(2)}
      </p>
    </div>
  );
};

export default SubscriptionTaxInfo;