// REPOSITORY SOURCE: diplomat-bit/almost | PATH: diplomat-bit-almost-93a5466/components/customers/CustomerLocation.tsx
================================================================================

import React from 'react';
import { Card, CardContent, Typography, Box } from '@mui/material';

interface CustomerLocationProps {
  address: string;
  city: string;
  state: string;
  zipCode: string;
  country: string;
}

const CustomerLocation: React.FC<CustomerLocationProps> = ({
  address,
  city,
  state,
  zipCode,
  country,
}) => {
  return (
    <Card elevation={3}>
      <CardContent>
        <Typography variant="h6" gutterBottom>
          Customer Location
        </Typography>
        <Box>
          <Typography variant="body1">
            <strong>Address:</strong> {address}
          </Typography>
          <Typography variant="body1">
            <strong>City:</strong> {city}
          </Typography>
          <Typography variant="body1">
            <strong>State:</strong> {state}
          </Typography>
          <Typography variant="body1">
            <strong>Zip Code:</strong> {zipCode}
          </Typography>
          <Typography variant="body1">
            <strong>Country:</strong> {country}
          </Typography>
        </Box>
      </CardContent>
    </Card>
  );
};

export default CustomerLocation;

================================================================================
// APPENDED FROM REPO: diplomat-bit/Fuckyou | ORIGINAL PATH: diplomat-bit-Fuckyou-70f83c5/components/customers/CustomerLocation.tsx
================================================================================

import React from 'react';
import { Card, CardContent, Typography, Box } from '@mui/material';

interface CustomerLocationProps {
  address: string;
  city: string;
  state: string;
  zipCode: string;
  country: string;
}

const CustomerLocation: React.FC<CustomerLocationProps> = ({
  address,
  city,
  state,
  zipCode,
  country,
}) => {
  return (
    <Card elevation={3}>
      <CardContent>
        <Typography variant="h6" gutterBottom>
          Customer Location
        </Typography>
        <Box>
          <Typography variant="body1">
            <strong>Address:</strong> {address}
          </Typography>
          <Typography variant="body1">
            <strong>City:</strong> {city}
          </Typography>
          <Typography variant="body1">
            <strong>State:</strong> {state}
          </Typography>
          <Typography variant="body1">
            <strong>Zip Code:</strong> {zipCode}
          </Typography>
          <Typography variant="body1">
            <strong>Country:</strong> {country}
          </Typography>
        </Box>
      </CardContent>
    </Card>
  );
};

export default CustomerLocation;

================================================================================
// APPENDED FROM REPO: diplomat-bit/jamesburvelocallaghaniiiand | ORIGINAL PATH: diplomat-bit-jamesburvelocallaghaniiiand-9414e97/components/customers/CustomerLocation.tsx
================================================================================

import React from 'react';
import { Card, CardContent, Typography, Box } from '@mui/material';

interface CustomerLocationProps {
  address: string;
  city: string;
  state: string;
  zipCode: string;
  country: string;
}

const CustomerLocation: React.FC<CustomerLocationProps> = ({
  address,
  city,
  state,
  zipCode,
  country,
}) => {
  return (
    <Card elevation={3}>
      <CardContent>
        <Typography variant="h6" gutterBottom>
          Customer Location
        </Typography>
        <Box>
          <Typography variant="body1">
            <strong>Address:</strong> {address}
          </Typography>
          <Typography variant="body1">
            <strong>City:</strong> {city}
          </Typography>
          <Typography variant="body1">
            <strong>State:</strong> {state}
          </Typography>
          <Typography variant="body1">
            <strong>Zip Code:</strong> {zipCode}
          </Typography>
          <Typography variant="body1">
            <strong>Country:</strong> {country}
          </Typography>
        </Box>
      </CardContent>
    </Card>
  );
};

export default CustomerLocation;

================================================================================
// APPENDED FROM REPO: diplomat-bit/magic | ORIGINAL PATH: diplomat-bit-magic-a3f5ff1/components/customers/CustomerLocation.tsx
================================================================================

import React from 'react';
import { Card, CardContent, Typography, Box } from '@mui/material';

interface CustomerLocationProps {
  address: string;
  city: string;
  state: string;
  zipCode: string;
  country: string;
}

const CustomerLocation: React.FC<CustomerLocationProps> = ({
  address,
  city,
  state,
  zipCode,
  country,
}) => {
  return (
    <Card elevation={3}>
      <CardContent>
        <Typography variant="h6" gutterBottom>
          Customer Location
        </Typography>
        <Box>
          <Typography variant="body1">
            <strong>Address:</strong> {address}
          </Typography>
          <Typography variant="body1">
            <strong>City:</strong> {city}
          </Typography>
          <Typography variant="body1">
            <strong>State:</strong> {state}
          </Typography>
          <Typography variant="body1">
            <strong>Zip Code:</strong> {zipCode}
          </Typography>
          <Typography variant="body1">
            <strong>Country:</strong> {country}
          </Typography>
        </Box>
      </CardContent>
    </Card>
  );
};

export default CustomerLocation;